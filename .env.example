# MBAX 6418 Assignment 1 - configuration template
#
# Copy this file to `.env` and fill in the values. `.env` is gitignored and
# must never be committed. The values come from `class-endpoints.txt` on
# Canvas (Dobolyi, 2026/09/09).
#
#     cp .env.example .env
#
# Then edit .env and set CLASS_API_KEY to the classroom token from Canvas.

CLASS_BASE_URL=http://dobolyi.com:9000/v1
CLASS_MODEL=DeepSeek-V4-Flash-0731
CLASS_API_KEY=

# The vision / classification endpoint, for later steps:
#   CLASS_BASE_URL=http://dobolyi.com:9001/v1
#   CLASS_MODEL=cyankiwi/Qwen3.6-35B-A3B-AWQ-4bit
